@page "/"
@using Refundation_App_Services.Extensions;
@using Refundation_App_Services.Services;
@using System.Net.Http.Json;
@using System.Reflection;
@inject IEmailSender e;

<FinalCalculations></FinalCalculations>








@code {
    /* protected override Task OnInitializedAsync()
        {
        List<Person> people = new List<Person>();
         Person p = new Person();
         p.Name = "Dragana";
         p.LastName = "Stefanovic";
         people.Add(p);
         Person p2 = new Person();
         p2.Name = "Milica";
         p2.LastName = "Stefanovic";
         people.Add(p2);
         e.sendMail("stefanoviic.dragana@gmail.com", "dragana.stefanovic@mercator.rs", "Test", people.ToHtmlTable());
    return null;


    }
    public class Person
        {
        public string Name{ get; set; }
        public String LastName { get; set; }
    }
    */
}